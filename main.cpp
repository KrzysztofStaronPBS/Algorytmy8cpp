#include <iostream>
#include "prototypy.h"
#define TABROZ 2000
int main() {
    int tabopt[TABROZ], tabrand[TABROZ], tabpes[TABROZ];
    odczyt_z_pliku(&tabrand[TABROZ]);
    for(int i : tabrand) {
    }
    return 0;
}